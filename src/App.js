import React from "react";

function App() {
   // you need to use className instead of class, htmlFor instead of for in JSX
   // you must have one div wrapping everyting
   return (
      <div>
         <h1>Hello World</h1>
         <p>This is my first React app</p>
      </div>
   );
}

export default App;
